import { Neutral } from '../theme';

// Стили для Handle
export const HandleStyles = {
  default: {
    backgroundColor: Neutral.grey100,
    borderRadius: 8,
    width: 48,
    height: 4,
    alignSelf: 'center',
  },
} as const;
