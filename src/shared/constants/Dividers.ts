import { Neutral } from '../theme';

// Стили для Divider
export const DividerStyles = {
  default: {
    width: '100%',
    height: 0,
    borderTopWidth: 1,
    borderTopColor: Neutral.grey100,
  },
} as const;
